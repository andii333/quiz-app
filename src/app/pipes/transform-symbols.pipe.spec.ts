import { TransformSymbolsPipe } from './transform-symbols.pipe';

describe('TransformSymbolsPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformSymbolsPipe();
    expect(pipe).toBeTruthy();
  });
});
